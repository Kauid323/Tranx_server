# TaruApp 配置文件示例
# 将此文件复制为 .env 并根据需要修改配置

# 服务器端口（默认：4999）
SERVER_PORT=4999

# 数据库路径（默认：./taruapp.db）
DATABASE_PATH=./taruapp.db

# 日志级别（可选：debug, info, warn, error）
LOG_LEVEL=info

# 最大分页大小（默认：100）
MAX_PAGE_SIZE=100

# 启用跨域支持（默认：true）
ENABLE_CORS=true

